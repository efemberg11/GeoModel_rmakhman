# GeoModelUtilities

```bash
git clone ssh://git@gitlab.cern.ch:7999/GeoModelDev/GeoModelUtilities.git
mkdir build_gmutils
cd build_gmutils
cmake -DCMAKE_INSTALL_PREFIX=../install -DCMAKE_BUILD_TYPE=RelWithDebInfo ../GeoModelUtilities/
make -j4
make install
```
